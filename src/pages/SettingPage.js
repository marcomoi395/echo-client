import React from 'react';
import {Login} from '../components';

const LoginPage = () => {
    return (
        <div>
            <h1>Setting</h1>
            <Login/>
        </div>
    );
};

export default LoginPage;
