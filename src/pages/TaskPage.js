import React from 'react';
import {Task} from '../components';

const TaskPage = () => {
    return (
        <div>
            <h1>Task</h1>
            <Task/>
        </div>
    );
};

export default TaskPage;
